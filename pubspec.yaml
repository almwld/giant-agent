name: giant_agent_nuclear
description: The Nuclear Giant Agent with Giant Database
version: 10.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  shared_preferences: ^2.2.0
  permission_handler: ^11.0.0
  share_plus: ^7.2.2
  file_picker: ^6.0.0
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
